FROM python:3.11-slim

WORKDIR /app

# Copy only simple API files
COPY simple_api.py .
COPY korean_flight_schedules.json .

# Install minimal dependencies
RUN pip install fastapi uvicorn

EXPOSE 8001

CMD ["uvicorn", "simple_api:app", "--host", "0.0.0.0", "--port", "8001"]